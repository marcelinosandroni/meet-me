# ✅ Fase 1 concluída — Setup & Base UI

**Entregue:** Tailwind v4 com tokens (Inter, dark por classe), Layout Header/Footer com toggle de tema, componentes UI base (Button, Input/Textarea, Card, Badge, Spinner, Skeleton), React Router (/ , /schedule/:hostId, /admin) e Zustand store persistente + DI container (core/di, DependencyProvider, useDi) conforme REACT.md.

**Validação:** `npm run typecheck` e `npm run build` 100% verdes.
